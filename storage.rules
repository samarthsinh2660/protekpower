rules_version = '2';

// Blog images: readable by visitors, writable only by signed-in staff.
// Deploy with:  firebase deploy --only storage
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
